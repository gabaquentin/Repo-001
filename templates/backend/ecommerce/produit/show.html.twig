{% extends 'backend/base/base.html.twig' %}

{% block stylesheets %}
    <!-- Plugins css -->
    <link href="{{ asset('backend/libs/datatables/responsive.bootstrap4.css') }}" rel="stylesheet" type="text/css" />
    <link href="{{ asset('backend/libs/datatables/dataTables.bootstrap4.css') }}" rel="stylesheet" type="text/css" />
{% endblock %}

{% block block_starter %} Gestion des Produits {% endblock %}
{% block body %}
    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-body">

                    <h4 class="header-title">Liste des produits</h4>
                    <p class="text-muted font-13 mb-4">
                    </p>
                    <div class="mb-lg-3">
                        <div class="row">
                            <div class="col-12 text-sm-center form-inline">
                                <div class="form-group mr-2">
                                    <a href="{{ path('add_produit') }}" class="btn btn-primary"><i class="mdi mdi-plus-circle mr-2"></i>Ajouter un produit</a>
                                </div>
                            </div>
                        </div>
                    </div>
                    <table id="datatable-buttons" class="table dt-responsive nowrap">
                        <thead class="bg-light">
                        <tr>
                            <th class="font-weight-medium">Id</th>
                            <th class="font-weight-medium">Image</th>
                            <th class="font-weight-medium">Nom</th>
                            <th class="font-weight-medium">Type Transaction</th>
                            <th class="font-weight-medium">Prix</th>
                            <th class="font-weight-medium">Promotion</th>
                            <th class="font-weight-medium">Localisation</th>
                            <th class="font-weight-medium">Création</th>
                            <th class="font-weight-medium">Action</th>
                        </tr>
                        </thead>

                        <tbody>

                        </tbody>
                    </table>

                </div> <!-- end card body-->
            </div> <!-- end card -->
        </div><!-- end col-->
    </div>
{% endblock %}

{% block javascripts %}
    <script>
        const GetProduitRoute = "{{ path('get_produit_back') }}";
        const modifyProduitRoute = "{{ path('modify_produit') }}";
        const addProduitRoute = "{{ path('add_produit') }}";
        const baseImageProduit = "/images/produits/";
        const deleteProduitRoute = "{{ path('delete_produit') }}"
    </script>
    <!-- Plugins Js -->
    <script src="{{ asset('backend/libs/datatables/jquery.dataTables.min.js') }}"></script>
    <script src="{{ asset('backend/libs/datatables/dataTables.bootstrap4.js') }}"></script>
    <script src="{{ asset('backend/libs/datatables/dataTables.buttons.min.js') }}"></script>
    <script src="{{ asset('backend/libs/datatables/buttons.bootstrap4.min.js') }}"></script>

    <!-- custom js-->
    <script src="{{ asset('backend/js/pages/ecommerce/produit-list.js') }}"></script>

{% endblock %}